{
  "name": "ports-and-adapters",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@testing-library/jest-dom": "4.1.2",
    "@testing-library/react": "9.3.0",
    "codeceptjs": "2.3.2",
    "puppeteer": "1.20.0",
    "react-scripts": "3.2.0"
  },
  "dependencies": {
    "axios": "0.21.1",
    "react": "16.10.2",
    "react-dom": "16.10.2",
    "serve": "11.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:production": "npm run build && serve -s build",
    "test": "react-scripts test unit",
    "test:acceptance": "npx codeceptjs run --steps",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
